// import React from 'react';
// import Badge from '../badge/Badge'; // The component you shared
// import { DreyfusLevel, DreyfusLevelBadgeType } from '../../lib/types';

// interface LevelBadgeProps {
//   dreyfusLevel: DreyfusLevel;
// }

// export function LevelBadge({ dreyfusLevel }: LevelBadgeProps) {
//   // Convert numeric enum to a string label
//   const label = DreyfusLevel[dreyfusLevel]; 
//   // e.g. "Proficient"

//   // Lookup the color from your badge type map
//   const type = DreyfusLevelBadgeType[dreyfusLevel]; 
//   // e.g. "yellow"

//   return <Badge label={label} type={type} />;
// }
